# External reference sources cloned for spec-by-example reading.
# Not part of this project — never committed.
.reference/

# Node
node_modules/
dist/
*.tsbuildinfo

# Rust (for fixture-gen/ later)
target/
Cargo.lock.bak

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Per-session ephemera. Tracks "where we are right now" between sessions but
# is rewritten every handoff — committing it would create churn and merge
# noise. Read it locally; never commit. CLAUDE.md is the stable companion.
SESSION_CONTEXT.md
